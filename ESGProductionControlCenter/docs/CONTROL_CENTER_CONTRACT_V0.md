# Control Center Output Contract v0

The Control Center writes only operational metadata, command plans, run logs,
contract validation results, CSV viewer state, and review editor exports.

It must not produce ESG scores, final indicators, or validated indicators.
