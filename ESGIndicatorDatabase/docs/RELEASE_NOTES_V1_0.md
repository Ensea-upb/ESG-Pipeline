# ESGIndicatorDatabase v1.0 - Release Notes

This release creates a preparation-only indicator database builder.

It supports empty accepted-candidate files and still writes valid empty outputs with an audit warning.

It does not produce final ESG indicators or scores.
